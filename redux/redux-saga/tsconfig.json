{
	"compilerOptions": {
		// 컴파일시 /dist에 생성되는 js 버전
		"target": "es5",

		// import 문법
		"module": "commonjs",

		// 컴파일시 포함될 라이브러리 목록
		"lib": ["dom", "ES2015", "ES2016", "ES2017", "ES2018", "ES2019", "ES2020"],

		// ts 컴파일시 js도 컴파일 할 것인가
		"allowJs": true,

		// tsx 파일을 jsx로 어떻게 컴파일할 것인지
		"jsx": "preserve",

		// 파일 output 경로
		"outDir": "./dist",

		// strict 관련, noimplicit 관련 모드 전부 on
		"strict": true,

		// module 해석 방법
		"moduleResolution": "node",

		// 모듈 이름을 처리할 기준 디렉토리
		"baseUrl": "./",

		// default export 사용하지 않은 모듈도 default import 가능하게 할 것인가
		"allowSyntheticDefaultImports": true,

		// import React = require("react") => import * as React from "react" => import React from "react"
		"esModuleInterop": true,

		// 같은 파일에 대한 일관되지 않은 참조 허용하지 않음
		"forceConsistentCasingInFileNames": true
	},
	"include": ["src"]
}
